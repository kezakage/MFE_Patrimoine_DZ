# Core
# Django pinned to the latest 5.0.x security patch (fixes PYSEC-2024-67/68/102/
# 156/157, PYSEC-2025-1/13/14 + GHSA SQL-injection/DoS advisories vs 5.0.6).
# NOTE: 5.0 is EOL — migrate to the 5.2 LTS line at the next maintenance window.
Django==5.0.14
djangorestframework==3.15.2  # GHSA-gw84-84pc-xp82 (XSS in browsable API)
django-filter==24.2
django-cors-headers==4.3.1

# Auth
djangorestframework-simplejwt==5.5.1  # GHSA-5vcc-86wm-547q (token info leak)
django-guardian==2.4.0
# TOTP two-factor authentication (RFC 6238)
pyotp==2.9.0
# Email confirmation flow — syntactic + DNS/MX validation on sign-up to reject
# typos (gmial.com) before sending the verification mail; rate-limiting on the
# resend endpoint to throttle abuse.
email-validator==2.2.0
django-ratelimit==4.1.0

# Social auth (OAuth2 / SSO) — django-allauth handles the provider dance,
# dj-rest-auth wraps it with REST endpoints that mint our SimpleJWT pair.
django-allauth==0.63.6
dj-rest-auth==6.0.0

# DB & GIS
psycopg[binary]==3.1.19
# PostGIS support comes built-in via django.contrib.gis

# Async / Realtime
channels==4.1.0
channels-redis==4.2.0
daphne==4.1.2

# Celery & Cache
celery==5.4.0
redis==5.0.4
django-redis==5.4.0

# Media / Storage
django-storages==1.14.3
boto3==1.34.106
Pillow==10.3.0

# API docs
drf-spectacular==0.27.2

# Search — Elasticsearch
django-elasticsearch-dsl==8.0
elasticsearch-dsl==8.13.1
elasticsearch==8.13.2

# PDF export — WeasyPrint produces PDF/A-1 from HTML/CSS
WeasyPrint==62.3
pydyf==0.10.0
pikepdf==9.1.1

# Chatbot RAG — pgvector storage + sentence-transformers embeddings + LangChain/Mistral
pgvector==0.3.6
sentence-transformers==3.0.1
langchain==0.2.16
langchain-mistralai==0.1.13

# Static files in production — WhiteNoise serves /static/ from the WSGI/ASGI
# process so we don't need a separate nginx in front of Daphne.
whitenoise==6.7.0

# Observability — Prometheus exporter for Django views/DB/cache
django-prometheus==2.3.1
# Sentry — production error & performance tracking (gated on SENTRY_DSN env)
sentry-sdk[django]==2.14.0

# Dev / Quality
python-dotenv==1.0.1
factory-boy==3.3.0
ipython==8.24.0
pytest==8.2.2
pytest-django==4.8.0
pytest-asyncio==0.23.7
pytest-cov==5.0.0
# Security — dependency CVE audit (CI) + static security linter
pip-audit==2.7.3
# Load testing — 50+ concurrent virtual users (loadtest/locustfile.py)
locust==2.31.6
